import React from 'react';
import {TextInput, View, Text } from 'react-native';

const Input = () => {
    return (
        <View>
            <Text>Testing</Text>
        </View>
    );
};

export default Input;